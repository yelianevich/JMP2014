rootProject.name = 'jmp'

include "jmp-jms", "jmp-spring-mvc", "jmp-java-core", "jmp-enterprise-design-patterns", "jmp-berlin-clock"

// set custom build files names for each subproject
rootProject.children.each {
	it.buildFileName = it.name + '.gradle' - 'jmp-'
}